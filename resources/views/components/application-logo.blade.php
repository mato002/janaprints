<img
    src="{{ url(config('site.local.logo')) }}"
    alt="{{ config('site.name') }}"
    {{ $attributes->merge(['class' => 'object-contain']) }}
    width="40"
    height="40"
    decoding="async"
>
